<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/DialogCardStyle"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            style="@style/DialogBodyStyle"
            android:orientation="vertical">

            <TextView
                android:id="@+id/dialog_sign_out_message"
                style="@style/DialogMessageStyle"
                android:text="@string/sign_out_message" />

        </LinearLayout>

        <LinearLayout
            android:id="@+id/filterBottomBar"
            style="@style/DialogActionAreaStyle">

            <Button
                android:id="@+id/dialog_sign_out_button_negative"
                style="@style/DialogActionButtonStyle"
                android:text="@android:string/cancel" />

            <Button
                android:id="@+id/dialog_sign_out_button_positive"
                style="@style/DialogActionButtonStyle"
                android:text="@string/sign_out" />

        </LinearLayout>

    </LinearLayout>

</android.support.v7.widget.CardView>